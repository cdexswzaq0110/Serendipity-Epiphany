# Skill 觸發評測

維護契約第 8 條（改 skill 要跑觸發測試）的落地處。

第 6 條的落地處是 [`.claude/ABLATION.md`](../../.claude/ABLATION.md)，第 8 條原本沒有——案例集埋在 `se-skill-authoring/references/eval-bar.md` 底下，不可重跑、不可比對。這個目錄補上那個缺口。

## 為什麼要有它

**改 description 是在動路由，而路由沒有量尺就只能憑感覺。** 改完覺得「這樣寫比較清楚」是最危險的判斷——清楚是給人看的，觸發是給模型用的，兩者不必然一致。

案例集的作用是**擋住明顯退步**，不是證明改善。10 條、手動、無統計顯著性——別拿它宣稱效果。

## 怎麼跑

1. **開一個乾淨 session**（不能沿用已經載入過 skill 的 session，前文會污染路由）。
2. 逐條輸入 [`trigger-cases.md`](trigger-cases.md) 的「使用者說的話」，**一字不改**。
3. 記錄模型實際載入了哪些 skill。
4. 對照「該載入／不該載入」欄，判 PASS / FAIL。
5. 在 `trigger-cases.md` 底部的執行紀錄新增一列，寫上日期、commit SHA、命中率。

**每條跑 2 次，兩次都對才算 PASS。** 這不是統計顯著，只是排除單次抖動。

## 判定

| 結果 | 條件 |
|---|---|
| **PASS** | 該載入的載入了，**且**「明確不該載入」欄的全都沒載入 |
| **FAIL** | 其他任何情況——記下實際載入了什麼，那才是有用的資訊 |

「都沒載入任何 skill」也是 FAIL（案例 1–8 都應該命中某個 skill）。

## 什麼時候必須跑

- 改任何 skill 的 `description`（維護契約 #8）
- 新增或刪除 skill
- 消融常駐規則之後——常駐面變少不該讓路由變差

## 限制（先講清楚，免得日後被當成證據）

- 10 條案例、手動執行、無統計顯著性
- 案例是自己寫的，會偏向自己的預期。**用真的說過的話，不要新造漂亮句子**
- 命中率相同不代表沒有退步，只代表這 10 條沒抓到
